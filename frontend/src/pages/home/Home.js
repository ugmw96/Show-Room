import Card from '../../shared/Card'

const Home = () => {
   
  return (
    <div>
      <Card title='Gayan' imageUrl='https://picsum.photos/200/300' description='React Router is the most popular routing library in React, but it can be a bit complicated to wrap your head around some of the more complex features. That is why in this article I will be breaking' price={233}/>
    </div>
  )
}

export default Home